name: Release

# Tag-driven. Pushing v1.2.3 builds the app, packages it, and publishes a
# GitHub Release with the artifacts attached.
on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g. 1.2.3) — dry run, does not publish'
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build & package
    runs-on: windows-latest
    timeout-minutes: 60

    # x64 only. The committed publish lock files (packages.publish.lock.json)
    # were generated for win-x64; an arm64 publish fails NU1004 on a changed
    # runtime identifier. Add arm64 here once those lock files are regenerated
    # for it — shipping a matrix leg that cannot restore is worse than shipping
    # one architecture and saying so.
    strategy:
      matrix:
        platform: [x64]

    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Resolve version
        id: version
        shell: pwsh
        run: |
          if ($env:GITHUB_REF -like 'refs/tags/v*') {
            $v = $env:GITHUB_REF -replace '^refs/tags/v',''
          } else {
            $v = '${{ inputs.version }}'
          }
          "version=$v" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          Write-Host "Building version $v"

      - name: Restore
        run: >
          dotnet restore ConnectOnion.WinUIClient/ConnectOnion.WinUIClient.sln
          -p:Platform=${{ matrix.platform }}

      - name: Build (Release)
        run: >
          dotnet build ConnectOnion.WinUIClient/ConnectOnion.WinUIClient.csproj
          --configuration Release
          -p:Platform=${{ matrix.platform }}
          -p:Version=${{ steps.version.outputs.version }}
          -p:PublishReadyToRun=false -p:PublishTrimmed=false

      # A normal Release build leaves untrimmed publish intermediates under obj.
      # Reusing them can silently produce an untrimmed archive even with
      # PublishTrimmed=true, so clean them and let publish restore its own graph.
      - name: Clean publish intermediates
        shell: pwsh
        run: >
          dotnet clean ConnectOnion.WinUIClient/ConnectOnion.WinUIClient.csproj
          --configuration Release
          --runtime win-${{ matrix.platform }}
          -p:Platform=${{ matrix.platform }}
          -p:BuildProjectReferences=false

      # NuGetLockFilePath: the publish graph has its own committed lock file per
      # project; validating it against the ordinary one fails NU1004.
      - name: Publish self-contained
        shell: pwsh
        run: >
          dotnet publish ConnectOnion.WinUIClient/ConnectOnion.WinUIClient.csproj
          --configuration Release
          --runtime win-${{ matrix.platform }}
          -p:Platform=${{ matrix.platform }}
          -p:RunUnpackaged=true
          -p:SelfContained=true
          -p:WindowsAppSDKSelfContained=true
          -p:PublishTrimmed=true
          -p:PublishReadyToRun=true
          -p:RuntimeIdentifiers=win-${{ matrix.platform }}
          -p:NuGetLockFilePath=packages.publish.lock.json
          -p:AppxPackageSigningEnabled=false
          -p:Version=${{ steps.version.outputs.version }}
          --output publish/${{ matrix.platform }}

      # Unsigned on purpose. Code signing needs a certificate in secrets; wire it
      # up here when one exists rather than shipping a placeholder that fails.
      - name: Package
        shell: pwsh
        run: |
          $v = '${{ steps.version.outputs.version }}'
          $name = "oochat-windows-$v-${{ matrix.platform }}"
          Compress-Archive -Path publish/${{ matrix.platform }}/* -DestinationPath "$name.zip"
          (Get-FileHash "$name.zip" -Algorithm SHA256).Hash | Out-File "$name.zip.sha256"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: oochat-windows-${{ matrix.platform }}
          path: |
            *.zip
            *.zip.sha256
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    timeout-minutes: 15

    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes \
            --verify-tag \
            artifacts/*.zip artifacts/*.zip.sha256
